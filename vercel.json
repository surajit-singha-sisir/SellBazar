{
  "version": 2,
  "buildCommand": "npm install --prefix client && cd client && npx --yes --ignore-existing vite build",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" },
    { "source": "/((?!api).*)", "destination": "/index.html" }
  ]
}
